function setup() {
  createCanvas(windowWidth, windowHeight);
  background(150);
  
  yellow(100, 100, 50);
}

function draw() {
  
}

function yellow(x, y, radius) {
  fill("yellow");
  circle(x, y, radius);
}