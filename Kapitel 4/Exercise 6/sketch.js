function setup() {
  createCanvas(400, 400);
    
}

function draw() {
  background(150);

  if (keyIsPressed) {
    if (key === "a" || key === "A") {
      line
    }

    if (key === "v" || key === "V")

  }

}
